<apex:page showHeader="false" sidebar="false" standardStylesheets="false" docType="html-5.0">
    <html>
    <head>
        <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <title>Rezerwacja Kortów Padel</title>

        <style>
            body {
                margin: 0;
                padding: 0;
                font-family: 'Salesforce Sans', Arial, sans-serif;
                background-color: #f3f3f3;
            }

            #lightning-container {
                min-height: 100vh;
                width: 100%;
            }

            .loading-spinner {
                display: flex;
                justify-content: center;
                align-items: center;
                min-height: 100vh;
                flex-direction: column;
                gap: 20px;
            }

            .spinner {
                border: 4px solid #f3f3f3;
                border-top: 4px solid #0176d3;
                border-radius: 50%;
                width: 40px;
                height: 40px;
                animation: spin 1s linear infinite;
            }

            @keyframes spin {
                0% { transform: rotate(0deg); }
                100% { transform: rotate(360deg); }
            }

            .error-message {
                max-width: 600px;
                margin: 50px auto;
                padding: 20px;
                background-color: #c23934;
                color: white;
                border-radius: 4px;
                text-align: center;
            }

            .error-message h2 {
                margin-top: 0;
            }

            .error-message button {
                margin-top: 15px;
                padding: 10px 20px;
                background-color: white;
                color: #c23934;
                border: none;
                border-radius: 4px;
                cursor: pointer;
                font-weight: bold;
            }
        </style>
    </head>
    <body>
        <!-- Loading indicator -->
        <!-- <div id="loading-indicator" class="loading-spinner">
            <div class="spinner"></div>
            <p>Ładowanie aplikacji...</p>
        </div> -->

        <!-- Container for Lightning component -->
        <div id="lightning-container"></div>

        <!-- Error container (hidden by default) -->
        <div id="error-container" style="display: none;">
            <div class="error-message">
                <h2>Błąd ładowania aplikacji</h2>
                <p id="error-message-text">Wystąpił problem podczas ładowania aplikacji. Spróbuj odświeżyć stronę.</p>
                <button onclick="window.location.reload();">Odśwież stronę</button>
            </div>
        </div>

        <!-- Include Lightning Out framework -->
        <apex:includeLightning/>

        <script>
                $Lightning.use("c:PadelBookingGuestApp", function() {
        $Lightning.createComponent("c:padelBookingApp", {},"lightning-container");
    });
            // Initialize Lightning Out
            // (function() {
            //     'use strict';

            //     // Hide loading spinner
            //     function hideLoading() {
            //         var loadingIndicator = document.getElementById('loading-indicator');
            //         if (loadingIndicator) {
            //             loadingIndicator.style.display = 'none';
            //         }
            //     }

            //     // Show error message
            //     function showError(message) {
            //         hideLoading();
            //         var errorContainer = document.getElementById('error-container');
            //         var errorText = document.getElementById('error-message-text');

            //         if (errorContainer && errorText) {
            //             errorText.textContent = message || 'Wystąpił nieznany błąd.';
            //             errorContainer.style.display = 'block';
            //         }

            //         console.error('Lightning Out Error:', message);
            //     }

            //     // Initialize the Lightning Out app
            //     try {
            //         $Lightning.use(
            //             "c:PadelBookingGuestApp",  // Aura app with ltng:allowGuestAccess
            //             function() {
            //                 // App loaded successfully, now create the LWC component
            //                 $Lightning.createComponent(
            //                     "c:PadelBookingContainer",  // Main LWC component
            //                     {},  // Component attributes (none needed)
            //                     "lightning-container",  // Target div ID
            //                     function(component) {
            //                         // Component created successfully
            //                         hideLoading();
            //                         console.log('Padel Booking App loaded successfully');
            //                     }
            //                 );
            //             },
            //             "{!$Site.BaseUrl}",  // Site URL (important for Guest User context)
            //             function(error) {
            //                 // Error loading the app
            //                 showError('Nie udało się załadować aplikacji: ' + error);
            //             }
            //         );
            //     } catch (e) {
            //         showError('Błąd inicjalizacji: ' + e.message);
            //     }

            //     // Timeout fallback (if loading takes more than 15 seconds)
            //     setTimeout(function() {
            //         var loadingIndicator = document.getElementById('loading-indicator');
            //         if (loadingIndicator && loadingIndicator.style.display !== 'none') {
            //             showError('Ładowanie aplikacji trwa zbyt długo. Sprawdź połączenie internetowe i spróbuj ponownie.');
            //         }
            //     }, 80000);
            // })();
        </script>
    </body>
    </html>
</apex:page>
